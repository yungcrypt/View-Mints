import { Container, Grid, ImageList, ImageListItem } from "@material-ui/core";


 export const SelectorBox = ({children}) => {
		return <>
			<div style={{backgroundColor:"none"}}>
				{children}
			</div>

		</>
}
